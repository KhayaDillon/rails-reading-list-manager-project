<h1>Your Shelves</h1>

<% @shelves.each do |shelf| %>
  <div>
    <h2> <%= shelf.name %> </h2>
    <ul>  
      <% shelf.books.each do |book| %>
        <li><%= book.title %> || <%= book.author %></li>
      <% end %>
    </ul>
  </div>
<% end %>
